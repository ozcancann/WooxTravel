﻿
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">

@Html.Action("PartialHead", "Default")

<body>

    @Html.Action("PartialNavbar", "Default")

    @Html.Action("PartialCountries", "Countries")

    @Html.Action("PartialFooter", "Default")

    @Html.Action("PartialScript", "Countries")

</body>

</html>